# Automation Libraries (Optional)

When needed for UI or system automation, consider:
- `pyobjc`, `appscript`, `pyautogui`
- `pynput`, `pillow`, `opencv`
- `requests` or `httpx` for network calls

Add any non-bundled dependencies to `pyproject.toml` under `code-agent`.
