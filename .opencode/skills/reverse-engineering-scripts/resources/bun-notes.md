# Bun Notes

Use `bun` for JavaScript/TypeScript scripts.

## Quick start
```bash
bun run scripts/extract_strings.ts --file <path>
```

## Tips
- Bun can run TypeScript directly.
- Keep scripts dependency-light unless necessary.
