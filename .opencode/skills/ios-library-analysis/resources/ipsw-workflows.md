# IPSW Workflows

Use the `ipsw` skill when you need to extract:
- dyld shared caches
- kernelcaches
- private frameworks
- firmware components

Reference `ipsw` for the exact extraction commands and supported formats.
